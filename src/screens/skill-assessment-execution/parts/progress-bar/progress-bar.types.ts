import { Color } from "@thekeytechnology/framework-react-components";

export type ProgressBarProps = {
	value: number;
	strokeWidth?: number;
	trailColor?: Color;
	pathColor?: Color;
	textColor?: Color;
};
