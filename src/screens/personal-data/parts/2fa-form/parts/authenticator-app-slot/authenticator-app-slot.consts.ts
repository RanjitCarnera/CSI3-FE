const title = "Authenticator app";
const subtitle =
	"Use an authentication app or browser extension to get two-factor authentication codes when prompted.\n";
export const authenticatorAppSlotTranslations = {
	title,
	subtitle,
};
